export type Hero = {
    id: number;
    name: string;
    description: string;
    imageUrl: string; // Cambiado de imageurl a imageUrl para coincidir con la BD
    createdAt: string;
    updatedAt: string;
}